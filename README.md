# Semantic-Search-With-FAISS
Building a Simple Semantic Search System with FAISS and Sentence Transformers

It's simple project using Facebooks Artificial Intelligence Semantic Search, It lays base foundation for vetor Database where the data stored are embeded numbers hiding meanings inside.
The system supports rebuilding the index from scratch, adding new documents, and running real-time semantic search through a simple FastAPI interface.
